# ── Python ─────────────────────────────────────────────────────────────────
__pycache__/
*.py[cod]
*$py.class
.venv/
venv/
env/

# ── Local collector state (per-machine, never commit) ──────────────────────
collector/state.json
collector/config.json
collector/config.local.json

# ── Build artifacts ────────────────────────────────────────────────────────
# PyInstaller output
installer/build/
installer/dist/
installer/*.spec

# Inno Setup output (the .exe ships as a GitHub Release, not in the repo)
installer/Output/

# ── Secrets ────────────────────────────────────────────────────────────────
installer/team_token.txt

# ── Editor / OS ────────────────────────────────────────────────────────────
.vscode/
.idea/
*.swp
.DS_Store
Thumbs.db

# ── Claude Code per-project state ──────────────────────────────────────────
.claude/
